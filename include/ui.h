#ifndef UI_H
#define UI_H

// No common.h inclusion needed here directly, as it only declares UI functions

// Function declarations for UI
void show_login_prompt();
void show_loggedin_prompt(const char* username);

#endif // UI_H
